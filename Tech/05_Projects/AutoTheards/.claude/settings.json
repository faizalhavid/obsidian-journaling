{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,subprocess,shutil; d=json.load(sys.stdin); inp=d.get('tool_input',{}); f=inp.get('file_path','') or inp.get('path',''); r=shutil.which('ruff'); subprocess.run(['ruff','check','--fix',f],capture_output=True) if (f and f.endswith('.py') and r) else None\""
          }
        ]
      }
    ]
  }
}
